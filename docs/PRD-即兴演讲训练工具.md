# PRD: 即兴演讲训练工具

> 当前阶段：确定要做
> 最后更新：2026-08-11

## 一、为什么做

### 目标用户
添叔自己（第一阶段是个人练习工具，不对外，先验证方法本身有没有用）。

### 使用场景
添叔在需要临场组织语言表达（比如突然被点名发言、面试、公开场合即兴发表观点）时，经常缺乏准备时间，容易语无伦次、逻辑散乱。他希望在真正开口前，先用一套结构化的提问卡片，花很短时间把思路搭个骨架，再对着骨架限时练习一遍。

### 用户需求/痛点
即兴表达最大的问题不是"没话说"，而是"想到哪说到哪，没有结构"。需要一个轻量、几分钟内能用完的工具，帮自己在开口前快速搭出一条逻辑线，而不是长篇大论地做准备。

### 支撑
🟡 中 — 添叔自己有过真实体感（被突然点名发言时语无伦次过），没有做过更大范围的调研或竞品分析。

### 收益/成功标准
定性指标：添叔自己愿意主动、反复打开这个工具练习（比如临场发言前会想起来用一下），而不是用一两次就放弃。暂不设精确的使用频次数字，先看这个工具是否真的进入日常使用习惯。

## 二、怎么做

### 功能范围 (MVP)

来自 [即兴演讲训练工具-backlog.md](即兴演讲训练工具-backlog.md) 已确认的 MVP 范围：

1. 随机话题生成器（Hot Takes 分类）
2. 18 张预设思路卡片（六个家族 × 3 张审问方法，文案见 [reference/思维训练卡/思维训练卡_18张卡片内容_MVP.md](../reference/思维训练卡/思维训练卡_18张卡片内容_MVP.md)）
3. 点击卡片加入右侧已选面板
4. 已选卡片可删除
5. 已选卡片可拖拽调整顺序
6. 同一张卡片可重复添加多次
7. 已选卡片显示顺序序号
8. 源卡片区"已选"视觉标识
9. 倒计时练习（1/2/3 分钟可选）
10. 自定义模式（MVP 唯一支持的练习模式）

**视觉方向阶段新增的几项**（原始 backlog 确认 MVP 范围时未包含，做视觉方向时补的，见下方关键决策）：

11. Header：动态标语"Think fast in ___"，四个场景自动轮播（已实现）
12. 话题生成器下方：中/英文语言切换按钮（只切换话题滚轮里的话题内容，18 张卡片和界面其余文案保持中文，已实现）
13. 话题生成器下方：日常话题/面试准备 话题分类切换按钮（已实现，激活了原本推到 Later 的 Interview Prep 话题类，先用 10 条通用面试问题打底，未按 backlog 里 Behavioral/Design/Technical 等细分类再拆分）

**明确不做（推到 Later，理由见 backlog）**：话题库扩展分类（科技/Finance/ELI5 等）、话题难度分级、Interview Prep 话题类按 Behavioral/Design/Technical 等细分（当前只有一份不分类的 10 条通用面试问题）、十位演讲高手的预设卡片顺序模式（内容已备好在 `reference/思维训练卡/思维训练卡_十位专家预设顺序.md`，等 MVP 验证完机制本身再做）。

### 页面结构
单页应用，左右两栏布局，不需要额外的导航结构：

- **顶部 Header**（高度 172px）：居中是动态标语"Think fast in ___"——固定文案"Think fast in"后面跟一个轮播场景词 + 对应插画图标，四个场景循环播放：**interviews / small talks / meetings / classrooms**（固定英文品牌元素）；两侧留白，无其他导航元素。
- **左栏**：顶部话题生成器——上方是话题滚轮（展示当前话题），下方一排 3 个按钮：中/英文语言切换、日常话题/面试准备切换、"随机生成"（触发滚轮转动）；再下方是 18 张卡片墙，按六个家族（溯因、推果、提炼、拆步、复现、定性）分组展示，每组 3 张。
- **右栏**：已选卡片面板，展示用户挑选出的卡片顺序列表，支持拖拽/删除/查看序号。

### 核心流程

1. 打开页面，点击「随机生成」，从 Hot Takes 话题池抽一个话题，显示在输入框位置。
2. 浏览 18 张思路卡片，点击任意卡片 → 该卡片以副本形式追加到右侧已选面板末尾，序号自动排到最后一位；源卡片本身保留在卡片墙上、进入"已选"视觉状态，可以继续被点击（重复添加）。
3. 在右侧面板里整理思路：拖拽调整已选卡片顺序（序号联动更新）、删除不需要的卡片、需要的话重复添加同一张卡片。
4. 思路整理好后，选择倒计时时长（1 / 2 / 3 分钟），开始计时。
5. 倒计时期间，按已选卡片的顺序即兴讲述。
6. 倒计时结束，不做提示音，自动返回第 3 步的选卡面板，已选卡片保留，可直接调整后再次开始（也可以在倒计时期间点「提前结束」提前返回）。

### 交互细节

**Header 动态标语**
- 固定文案"Think fast in"+ 一个轮播场景词 + 该场景对应的插画图标（图标随场景词一起切换）。四个场景固定顺序循环：interviews → small talks → meetings → classrooms → interviews……
- 每 3 秒自动切换一次，无需用户交互触发；切换时场景词+图标一起做一次淡出淡入（约 250ms），不是硬切。
- 标语固定用英文（当作品牌元素处理，类似"Hot Takes"这类不翻译的专有名词），不跟随语言切换按钮变化。
- 素材来源：`reference/hand-drawn-essential-mini-illustrations-boldline-chapter-2-previews/` 里挑的四张手绘小图标，跟卡片墙、其余 UI 的极简黑白灰风格是两套视觉语言，只用在这一处做点缀。

**话题生成器**
- 上方是话题滚轮，展示当前话题（老虎机式滚动效果，机制仿照 speechtopicgen.com）；下方一排 3 个按钮，从左到右依次是：中/英文语言切换、日常话题/面试准备切换、"🎲 随机生成"。
- 点击「随机生成」从当前选中的话题池（日常话题 Hot Takes / 面试准备 Interview Prep）随机抽取一条，滚轮转动后停在这一条上；可重复点击换一个话题，无需额外确认。
- 语言切换按钮：**只切换话题滚轮里的内容**——话题库（日常话题/面试准备两个分类）的话题文本会跟着切换中英文。18 张卡片上的问题文本、家族名（溯因/推果…）、方法名，以及界面里所有的按钮文字、面板标题、空状态提示、时长选项、toast 提示等 UI 文案，始终保持中文，不受这个按钮影响。按钮文字显示的是"切换后会变成的语言"（当前中文时按钮显示"EN"，反之显示"中文"）。Header 的"Think fast in ___"标语也不受此按钮影响。
- 话题分类切换按钮：在"日常话题"（Hot Takes 风格的观点类话题）和"面试准备"（Interview Prep 风格的面试问答类话题）之间切换；按钮文字同样显示"切换后会变成的分类"。切换分类后滚轮重置为占位提示状态，需要重新点「随机生成」抽话题。

**卡片墙 → 已选面板**
- 点击卡片 = 复制一份加入右侧列表末尾，原卡片不移除、可重复点击。
- 已选面板每项前显示阿拉伯数字序号（1、2、3…），拖拽排序后序号跟随位置实时重新计算。
- 每项提供删除入口（如 hover 时出现的删除按钮），删除后后续序号自动前移，不留空位。
- 源卡片被选中后要有区别于未选中状态的视觉样式，让用户一眼看出墙上哪些已经用过。
- 重复添加同一张卡片时，源卡片左下角用小圆点表示被选次数（点几次出现几个点），不使用数字角标——数字角标和右侧已选面板的序号圆圈视觉上容易混淆。

**倒计时**
- 提供 1 / 2 / 3 分钟三个选项，用户选定后开始计时。
- 倒计时进行中，大数字倒计时下方直接展示当前话题文本（即开始练习时话题滚轮上停留的那条话题，不加"抽中的问题"之类的标签前缀），再下方完整展示已选卡片的顺序列表（只读，不可拖拽/删除/重新添加），方便照着讲。
- 提供「提前结束」入口，随时可以手动提前结束。
- 倒计时到 0（或点了提前结束）：不做提示音，直接自动返回选卡面板，已选卡片和话题保留，不弹出单独的"结束"确认画面。

**历史记录**
- 不做历史记录、不做本地持久化。刷新页面后话题和已选卡片都会重置，下次要重新抽话题、重新选卡。这是确认过的 MVP 范围决定，不是待定项。

## 三、给 AI 的补充（vibe coding）

> 核心流程的交互细节已经全部确认完，这部分暂时还是留白——等你说"开始写代码了"，回来把技术假设、数据模型、验收标准这三部分补全。

### 技术假设
暂不确定，留待准备开发阶段。

### 数据模型
暂不确定，留待准备开发阶段。

### 验收标准
暂不确定，留待准备开发阶段。

## 四、关键决策

- 2026-08-10 决策：MVP 阶段只做"自定义模式"，"十位专家预设顺序模式"推迟到 Later。原因：先验证"结构化提问卡片能不能帮上忙"这个核心机制本身，专家模式是在机制验证成功之后锦上添花的第二种玩法。
- 2026-08-10 决策：目标用户先定为添叔自己一个人，收益标准是"自己愿意主动使用"。原因：这是个人项目，先用自己验证价值，跑通了再考虑要不要开放给添叔生态的学员或更广泛用户。
- 2026-08-10 决策：重复选卡的视觉表现用左下角小圆点计数，不用数字角标。原因：数字角标和已选面板的序号圆圈容易在视觉上混淆成"顺序"，圆点只表达"次数"，语义更清楚。
- 2026-08-10 决策：倒计时进行中保留已选卡片列表可见（只读）；倒计时结束不做提示音，直接自动返回选卡面板，不做单独的"结束"确认画面；不做历史记录/本地持久化。原因：这是个人练习工具，流程越轻越好，先验证核心机制，持久化和更精细的结束反馈等确认有用了再加。
- 2026-08-11 决策：Header 加一条动态标语"Think fast in ___"，场景词确定为 interview、small talks、meetings、classrooms 四个。原因：先脑暴了 interview/meetings/negotiations/networking/debates 等职场向场景，又补了 toasts/classrooms/panels/client calls/press/dates 等更生活化的场景；最终选定的四个覆盖了面试、社交、职场会议、课堂四类不同情境，且长度、语气统一。曾考虑用"improv"替代 classrooms，但 improv 本身已经是"临场发挥"的意思，跟"Think fast"语义重复，最终弃用。
- 2026-08-11 决策：Header 高度定为 172px，去掉原来居中的"即兴演讲"产品名文字，改由"Think fast in ___"标语独占中间位置；标语每 3 秒自动轮播、固定顺序循环、不跟随语言切换；每个场景配一张 `reference/hand-drawn-essential-mini-illustrations-boldline-chapter-2-previews/` 里的手绘图标。原因：产品名文字和新标语同时居中会挤、主次不清，标语本身已经足够清楚地传达产品是做什么的，先去掉产品名；轮播机制按用户给的具体参数直接定稿，不再走"待确认"流程。
- 2026-08-11 决策：移除 Header 右侧的中/英文语言切换按钮及其全部相关功能（双语文案对象、话题库/卡片文案的中英映射），全站统一为中文。原因：用户明确要求去掉，语言切换不是 MVP 范围内确认过的功能，只是视觉方向阶段顺手加的，产品定位（添叔自用中文练习工具）本身也不需要多语言。标语"Think fast in ___"保留固定英文，因为它一直是独立于语言切换的品牌元素。
- 2026-08-11 决策：话题生成器改版——话题滚轮下方新增一排 3 个按钮（中/英文语言切换、日常话题/面试准备切换、随机生成），原本在滚轮右侧的圆形"随机生成"按钮删除，功能合并进新的第三个按钮；语言切换按钮以新形式重新加入产品。同时激活了 backlog 里推到 Later 的"Interview Prep 话题类"，先用 10 条通用面试问题（自我介绍、项目成就、冲突处理、职业动机等）打底，不再按 Behavioral/Design/Technical 等细分类拆分话题池。原因：用户直接给出这个新布局的具体指令；面试准备场景和日常话题场景需要的话题库不同，加一个分类切换比强行合并成一个话题池更清楚。
- 2026-08-11 决策：语言切换按钮的范围先收窄为"只切换问题文本"（话题库 + 18 张卡片的问题），随后用户进一步明确只需要切换话题滚轮里的内容，18 张卡片的问题也不需要跟着切换。最终范围：**只有话题库（滚轮里显示的话题）会中英文切换，18 张卡片和界面其余文案始终保持中文**。原因：用户两次明确缩小范围，这是添叔自用的中文练习工具，真正需要偶尔切英文的只是"话题"本身（比如想用英文话题练习），18 张卡片是固定的方法论工具，没必要跟着切换。
- 2026-08-11 决策：倒计时界面的说明文案从通用的"倒计时进行中 · 按下面的顺序即兴讲述"改成展示当次抽中的具体话题——"抽中的问题：〈话题内容〉"。原因：用户指出倒计时时最需要看到的信息是"自己抽到的是哪个话题"，通用说明文字价值不大；这也补上了此前的一个体验缺口——话题只在开始练习前的滚轮里出现过，进入倒计时后就再也看不到了。

## 五、变更记录

### 2026-08-10
改了什么：基于已确认的 backlog MVP 范围，创建首版 PRD。
为什么改：MVP 范围确认后，进入"确定要做"阶段，把产品目标、核心流程、功能边界梳理清楚。
影响的部分：全部（首版）。

### 2026-08-10（低保真原型阶段）
改了什么：确定"重复选卡视觉标识"的方案（源卡片左下角小圆点计数），从待确认列表移入交互细节正文；对应更新低保真原型（`../prototype/low-fi/`）。
为什么改：做低保真原型时，发现数字角标和已选面板序号视觉冲突，改用圆点后一并拍板。
影响的部分：二、怎么做 - 交互细节；四、关键决策。

### 2026-08-10（剩余待确认问题拍板）
改了什么：确认倒计时进行中/结束的具体表现（进行中列表可见、结束自动返回不做提示音和确认画面）、确认不做历史记录；核心流程第 6 步、交互细节里的"以下细节暂不展开"清空，全部转为正式内容；对应更新低保真原型两个文件。
为什么改：backlog 里剩下的 5 个待确认问题，用户当面拍板了其中 3 个（倒计时进行中、倒计时结束、历史记录），只剩专家模式可编辑性和增长获客两项还开着。
影响的部分：二、怎么做 - 核心流程、交互细节；四、关键决策。

### 2026-08-11（补记 Header 相关内容）
改了什么：补充记录视觉方向阶段已经做的 Header 产品名+语言切换按钮（此前实现时漏了同步文档）；新增 Header 动态标语"Think fast in ___"的功能条目、场景词决策（interview/small talks/meetings/classrooms）、待确认的轮播机制细节。
为什么改：Header 的这几处改动都是在做低保真/视觉原型时直接决定并实现的，没有走"先更新 backlog/PRD 再实现"的流程，现在补记，让文档跟实际原型对齐。
影响的部分：二、怎么做 - 功能范围、页面结构、交互细节；四、关键决策。

### 2026-08-11（Header 标语实现定稿）
改了什么：拍板轮播机制的所有待确认项——3 秒自动轮播、固定顺序循环、切换时淡出淡入、标语不跟随语言切换；去掉 Header 里原来居中的"即兴演讲"产品名，改由标语独占中间位置；Header 高度定为 172px；每个场景配一张手绘插画图标。已在 `prototype/visual/index.html` 里实现并验证。
为什么改：这些是用户直接给的具体实现参数（高度、间隔、四个场景对应的图标文件），不再是"待确认"，直接定稿更新进文档。
影响的部分：二、怎么做 - 功能范围、页面结构、交互细节；四、关键决策。

### 2026-08-11（移除语言切换按钮）
改了什么：移除 Header 右侧的中/英文语言切换按钮和相关功能，全站文案、18 张卡片、话题库统一改回纯中文；`prototype/visual/index.html` 里对应删掉了双语数据结构和切换逻辑。
为什么改：用户明确要求去掉；该功能本来就是视觉方向阶段顺手加的，不在原始 MVP 范围内。
影响的部分：二、怎么做 - 功能范围、页面结构、交互细节；四、关键决策。

### 2026-08-11（话题生成器加按钮行，语言切换回归）
改了什么：话题滚轮下方新增 3 个按钮（语言切换、日常话题/面试准备切换、随机生成），删除滚轮右侧原来的圆形随机生成按钮；语言切换按钮以新位置、新样式重新加入；新增"面试准备"话题分类及 10 条中英文面试问题内容。`prototype/visual/index.html` 里重建了双语数据结构（这次额外加了 daily/interview 两套话题池），并同步更新。
为什么改：用户直接给出新布局指令；同时借这次改动把 backlog 里推到 Later 的 Interview Prep 话题类先做一版最小实现，验证话题分类切换这个交互本身。
影响的部分：二、怎么做 - 功能范围、页面结构、交互细节；四、关键决策。

### 2026-08-11（语言切换范围收窄为只切问题）
改了什么：语言切换按钮不再切换整站文案，只切换话题库和 18 张卡片的问题文本；`prototype/visual/index.html` 里把家族名、方法名、以及所有按钮/面板/toast 文案改回固定中文（不再是 `{zh,en}` 结构），只保留话题库和卡片 `question` 字段的双语数据。
为什么改：用户明确要求，全站切语言没必要，练习工具真正需要切换的只是"问题"本身。
影响的部分：二、怎么做 - 功能范围、交互细节；四、关键决策。

### 2026-08-11（语言切换范围再收窄为只切话题滚轮）
改了什么：语言切换按钮的范围进一步收窄——18 张卡片的问题文本也改回固定中文，`question` 字段不再是 `{zh,en}` 结构；只有话题库（`TOPICS`）还保留 `daily/interview × zh/en` 的双语数据。现在语言切换按钮只影响话题滚轮里显示的内容。
为什么改：用户进一步明确，只需要滚轮里的话题内容切英文，页面其他文字（包括卡片问题）都要保持原样。
影响的部分：二、怎么做 - 功能范围、交互细节；四、关键决策。

### 2026-08-11（倒计时界面显示抽中的话题）
改了什么：倒计时界面大数字下方的说明文案，从固定的"倒计时进行中 · 按下面的顺序即兴讲述"改成动态展示当前话题文本，取当前话题滚轮停留的那条话题。`prototype/visual/index.html` 里在 `startPractice()` 里读取 `TOPICS[state.topicMode][state.lang][state.topicIndex]` 并写入该文案；先加了"抽中的问题："前缀，随后按用户要求去掉前缀，只留话题文本本身。
为什么改：用户反馈倒计时时应该能看到自己抽到的话题；这也修复了话题在进入倒计时后就不再显示的体验缺口。去掉标签前缀是因为内容本身已经足够清楚，标签是多余的。
影响的部分：二、怎么做 - 交互细节；四、关键决策。

## 六、名词解释

- **Hot Takes**：话题库里的一个分类，指带一定观点争议性、适合快速表态的话题。
- **18 张思路卡片**：六个"审问方法"家族（溯因、推果、提炼、拆步、复现、定性）各 3 张，每张对应一种引导即兴演讲逻辑的提问方式。
- **家族**：18 张卡片的分类单位，每个家族代表组织思路的一个角度。
- **自定义模式**：MVP 唯一支持的练习模式，用户自己挑选、排序卡片；相对于 Later 阶段的"专家预设顺序模式"。
